package com.musio.memory.context;

public enum MemoryType {
    TASK_MEMORY,
    PROFILE_MEMORY,
    BEHAVIOR_SUMMARY,
    MUSIC_CACHE,
    CONVERSATION_SUMMARY,
    CURRENT_STATE,
    PENDING_ACTION
}
